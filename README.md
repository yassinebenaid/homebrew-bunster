# Homebrew tap for `bunster`
This is the homebrew tap used to install [the bunster programming language](https://bunster.netlify.app)

run:

```sh
brew tap yassinebenaid/bunster
brew install bunster
```
